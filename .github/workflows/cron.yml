name: Cron Job

on:
  schedule:
    - cron: "*/1 * * * *"

jobs:
  trigger-cron-job:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Cron Job
        run: |
          curl -X GET https://quander-watch.vercel.app/api/subscriptions
